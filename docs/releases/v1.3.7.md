# v1.3.7

- Sửa layout button/toolbar tự co giãn theo chiều rộng cửa sổ và DPI; hàng nút tự xuống dòng và tăng chiều cao, không yêu cầu kéo/chỉnh cửa sổ để hiện đủ nút.
- ADMIN được chọn một, nhiều hoặc toàn bộ phiếu đã nhập để xoá; bắt buộc xác minh lại mật khẩu hiện tại trước khi thực hiện.
- Xoá phiếu ghi dấu tombstone lên Google trước rồi mới xoá local để tránh dữ liệu cũ quay lại khi bổ sung đồng bộ đa máy.
- Chặn tạo phiếu mới khi toàn bộ trường nghiệp vụ và bộ ảnh (SHA-256) trùng hoàn toàn với phiếu local đã có.
